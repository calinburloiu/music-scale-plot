{
  "name": "music-scale-plot",
  "version": "0.1.0",
  "description": "Client-side web app for plotting microtonal music scales",
  "private": true,
  "license": "Apache-2.0",
  "scripts": {
    "test": "node --test \"test/**/*.test.js\"",
    "test:watch": "node --test --watch \"test/**/*.test.js\"",
    "test:coverage": "node --test --experimental-test-coverage \"test/**/*.test.js\""
  },
  "devDependencies": {
    "jsdom": "^27.0.0"
  }
}
